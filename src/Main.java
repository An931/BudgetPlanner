public class Main {
    public void Main(){
        BudgetPlanner BP = new BudgetPlanner();
        BP.createAllAreas();
        while (){
            //read transaction from input, add them
            BP.createTransaction(...);
        }
    }
}
